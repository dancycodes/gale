{
  "name": "dancycodes/gale",
  "version": "0.5.2",
  "description": "Laravel-native reactive frontends using Alpine Gale. Build dynamic UIs with Blade templates and Server-Sent Events.",
  "keywords": [
    "laravel",
    "alpine",
    "alpinejs",
    "reactive",
    "galemedia",
    "sse",
    "server-sent-events",
    "spa",
    "blade",
    "real-time",
    "fragments",
    "htmx-alternative",
    "livewire-alternative"
  ],
  "homepage": "https://github.com/dancycodes/gale",
  "type": "library",
  "license": "MIT",
  "authors": [
    {
      "name": "DancyCodes",
      "email": "dancycodes@gmail.com",
      "role": "Developer"
    }
  ],
  "support": {
    "issues": "https://github.com/dancycodes/gale/issues",
    "source": "https://github.com/dancycodes/gale"
  },
  "require": {
    "php": "^8.2",
    "illuminate/support": "^11.0|^12.0",
    "symfony/finder": "^5.4.2|^6.0|^7.0"
  },
  "require-dev": {
    "phpunit/phpunit": "^11.0 || ^12.0",
    "pestphp/pest": "^4.0",
    "orchestra/testbench": "^9.0 || ^10.0",
    "mockery/mockery": "^1.6",
    "fakerphp/faker": "^1.23",
    "larastan/larastan": "^3.0",
    "laravel/pint": "^1.0"
  },
  "autoload": {
    "psr-4": {
      "Dancycodes\\Gale\\": "src/"
    },
    "files": [
      "src/helpers.php"
    ]
  },
  "autoload-dev": {
    "psr-4": {
      "Dancycodes\\Gale\\Tests\\": "tests/"
    }
  },
  "scripts": {
    "test": "vendor/bin/pest",
    "test-coverage": "vendor/bin/pest --coverage-html coverage",
    "test-unit": "vendor/bin/pest --testsuite Unit",
    "test-feature": "vendor/bin/pest --testsuite Feature",
    "analyse": "vendor/bin/phpstan analyse --memory-limit=2G",
    "analyse:baseline": "vendor/bin/phpstan analyse --generate-baseline --memory-limit=2G",
    "format": "vendor/bin/pint",
    "format:test": "vendor/bin/pint --test",
    "ci": [
      "@format:test",
      "@analyse",
      "@test"
    ],
    "post-install-cmd": [
      "@php -r \"if (file_exists('artisan')) { echo shell_exec('php artisan view:clear 2>&1') ?: 'View cache cleared'; }\""
    ],
    "post-update-cmd": [
      "@php -r \"if (file_exists('artisan')) { echo shell_exec('php artisan view:clear 2>&1') ?: 'View cache cleared'; }\""
    ]
  },
  "extra": {
    "laravel": {
      "providers": [
        "Dancycodes\\Gale\\GaleServiceProvider"
      ]
    }
  },
  "config": {
    "allow-plugins": {
      "pestphp/pest-plugin": true
    }
  }
}
